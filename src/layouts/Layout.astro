---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content="Horizon Impact : solutions web sur-mesure, accompagnement humain, résultats concrets. Agence digitale à impact positif." />
		<meta property="og:title" content={title} />
		<meta property="og:description" content="Horizon Impact : solutions web sur-mesure, accompagnement humain, résultats concrets. Agence digitale à impact positif." />
		<meta property="og:type" content="website" />
		<meta property="og:image" content="/logo.png" />
		<meta property="og:locale" content="fr_FR" />
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content="Horizon Impact : solutions web sur-mesure, accompagnement humain, résultats concrets. Agence digitale à impact positif." />
		<meta name="twitter:image" content="/logo.png" />
	</head>
	<body class="min-h-screen bg-gray-50">
		<header style="position: fixed; top: 0; left: 0; right: 0; background: #fff; box-shadow: 0 2px 8px #1a237e11; z-index: 1000; padding: 1rem;">
			<nav style="max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center; padding: 0 1rem;">
				<a href="/" style="display: flex; align-items: center; gap: 0.7rem; text-decoration: none;">
					<img src="/logos/logo.png" alt="Logo Horizon D'Impact 3.0" style="height: 2.5rem; width: auto; border-radius: 0.7rem; background: #f5f5f5; box-shadow: 0 2px 8px #1a237e22; padding: 0.2rem;" />
					<span style="color: #1a237e; font-weight: 800; font-size: 1.5rem;">Horizon D'Impact 3.0</span>
				</a>
				
				<!-- Menu Hamburger pour Mobile -->
				<button id="menuButton" style="display: none; background: none; border: none; cursor: pointer; padding: 0.5rem;">
					<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
						<line x1="3" y1="12" x2="21" y2="12"></line>
						<line x1="3" y1="6" x2="21" y2="6"></line>
						<line x1="3" y1="18" x2="21" y2="18"></line>
					</svg>
				</button>

				<!-- Navigation Desktop -->
				<div id="desktopNav" style="display: flex; gap: 2rem;">
					<a href="/" style="color: #1a237e; text-decoration: none; font-weight: 500;">Accueil</a>
					<a href="/services" style="color: #1a237e; text-decoration: none; font-weight: 500;">Services</a>
					<a href="/a-propos" style="color: #1a237e; text-decoration: none; font-weight: 500;">À propos</a>
					<a href="/contact" style="color: #1a237e; text-decoration: none; font-weight: 500;">Contact</a>
				</div>

				<!-- Navigation Mobile -->
				<div id="mobileNav" style="display: none; position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: #fff; padding: 5rem 2rem 2rem 2rem; z-index: 999;">
					<button id="closeMenu" style="position: absolute; top: 1rem; right: 1rem; background: none; border: none; cursor: pointer; padding: 0.5rem;">
						<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
							<line x1="18" y1="6" x2="6" y2="18"></line>
							<line x1="6" y1="6" x2="18" y2="18"></line>
						</svg>
					</button>
					<div style="display: flex; flex-direction: column; gap: 1.5rem;">
						<a href="/" style="color: #1a237e; text-decoration: none; font-weight: 500; font-size: 1.2rem;">Accueil</a>
						<a href="/services" style="color: #1a237e; text-decoration: none; font-weight: 500; font-size: 1.2rem;">Services</a>
						<a href="/a-propos" style="color: #1a237e; text-decoration: none; font-weight: 500; font-size: 1.2rem;">À propos</a>
						<a href="/contact" style="color: #1a237e; text-decoration: none; font-weight: 500; font-size: 1.2rem;">Contact</a>
					</div>
				</div>
			</nav>
		</header>

		<main style="margin-top: 4rem;">
			<slot />
		</main>

		<footer style="background: #1a237e; color: #fff; padding: 3rem 1rem;">
			<div style="max-width: 1200px; margin: 0 auto;">
				<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; margin-bottom: 2rem;">
					<div>
						<h3 style="font-size: 1.2rem; font-weight: 700; margin-bottom: 1rem;">Horizon Impact</h3>
						<p style="color: #e0e0e0;">Accompagnement des PME et formation des jeunes talents.</p>
					</div>
					<div>
						<h3 style="font-size: 1.2rem; font-weight: 700; margin-bottom: 1rem;">Navigation</h3>
						<div style="display: flex; flex-direction: column; gap: 0.5rem;">
							<a href="/" style="color: #e0e0e0; text-decoration: none;">Accueil</a>
							<a href="/services" style="color: #e0e0e0; text-decoration: none;">Services</a>
							<a href="/a-propos" style="color: #e0e0e0; text-decoration: none;">À propos</a>
							<a href="/contact" style="color: #e0e0e0; text-decoration: none;">Contact</a>
						</div>
					</div>
					<div>
						<h3 style="font-size: 1.2rem; font-weight: 700; margin-bottom: 1rem;">Contact</h3>
						<p style="color: #e0e0e0;">Email: horizon.dimpact.3.0@gmail.com</p>
						<p style="color: #e0e0e0;">Tél: +237 6 99 94 20 02</p>
						<div style="margin-top: 1rem;">
							<iframe 
								src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3979.7536223150963!2d9.7053!3d4.0516!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zNMKwMDMnMDUuOCJOIDnCsDQyJzE5LjEiRQ!5e0!3m2!1sfr!2scm!4v1635000000000!5m2!1sfr!2scm" 
								width="100%" 
								height="200" 
								style="border:0; border-radius: 8px;" 
								allowfullscreen="" 
								loading="lazy"
							></iframe>
						</div>
					</div>
				</div>
				<div style="text-align: center; padding-top: 2rem; border-top: 1px solid #3949ab;">
					<p style="color: #e0e0e0;">© 2024 Horizon Impact. Tous droits réservés.</p>
				</div>
			</div>
		</footer>
	</body>
</html>

<script>
	// Gestion du menu mobile
	const menuButton = document.getElementById('menuButton');
	const closeMenu = document.getElementById('closeMenu');
	const mobileNav = document.getElementById('mobileNav');
	const desktopNav = document.getElementById('desktopNav');

	// Fonction pour gérer l'affichage du menu
	function toggleMenu() {
		mobileNav.style.display = mobileNav.style.display === 'none' ? 'block' : 'none';
		document.body.style.overflow = mobileNav.style.display === 'block' ? 'hidden' : '';
	}

	// Événements pour le menu
	menuButton?.addEventListener('click', toggleMenu);
	closeMenu?.addEventListener('click', toggleMenu);

	// Gestion du responsive
	function handleResize() {
		if (window.innerWidth <= 768) {
			menuButton.style.display = 'block';
			desktopNav.style.display = 'none';
		} else {
			menuButton.style.display = 'none';
			desktopNav.style.display = 'flex';
			mobileNav.style.display = 'none';
			document.body.style.overflow = '';
		}
	}

	// Initialisation et écoute du redimensionnement
	handleResize();
	window.addEventListener('resize', handleResize);

	// Animation des sections
	const observer = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				entry.target.style.opacity = '1';
				entry.target.style.transform = 'translateY(0)';
			}
		});
	}, {
		threshold: 0.1
	});

	document.querySelectorAll('[data-animate]').forEach((element) => {
		element.style.opacity = '0';
		element.style.transform = 'translateY(20px)';
		element.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out';
		observer.observe(element);
	});
</script>

<style>
	/* Styles globaux */
	:root {
		--primary: #1a237e;
		--secondary: #3949ab;
		--accent: #ffb300;
		--text: #333;
		--light: #fff;
		--gray: #e0e0e0;
	}

	body {
		margin: 0;
		font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
		line-height: 1.5;
		color: var(--text);
	}

	/* Styles du menu mobile */
	@media (max-width: 768px) {
		#menuButton {
			display: block !important;
		}
		#desktopNav {
			display: none !important;
		}
		#mobileNav {
			display: none;
		}
		#mobileNav.active {
			display: block;
		}
	}

	/* Styles des liens */
	a {
		transition: color 0.2s;
	}
	a:hover {
		color: var(--accent) !important;
	}

	/* Styles des boutons */
	button {
		transition: opacity 0.2s;
	}
	button:hover {
		opacity: 0.8;
	}
</style>
